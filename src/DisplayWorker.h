#include <Arduino.h>

class DisplayWorker {
public:
    // Constructor
    DisplayWorker();

    // Destructor
    ~DisplayWorker();

    // Public member functions
    void init();
    

private:
    // Private member variables

    // Private member functions
};